"""aiOS Director — the always-on coordinator on the Linux box.

Runs the chat, dispatches the work, and keeps the memory. The phone PWA and the
Windows aiOS desktop are both clients of this package.
"""
from __future__ import annotations

__version__ = "1.0.0"
