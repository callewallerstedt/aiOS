# aiOS Phone

Deploy:

```powershell
npx vercel login
npx vercel deploy --prod --yes
```

Open with a reachable HTTPS aiOS backend:

```text
https://your-vercel-app.vercel.app/?backend=https://your-computer-tunnel.example.com
```
